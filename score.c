#include <stdio.h>

int score(int O, int +) {
    if(O==1)
    {
        int nx = 0, ny = 0;
        int x = 2, y = 10;
        if(+==1)
        {
            y= y +10;
        }
        int monTableau[x][y];
        for (ny = 0; ny < y; ny++)
        {
            nx = 0;
            \\
            appelle pseudo
            \\
            nx++;
            \\
            appelle score
            \\
            nx = 0;
        }
        for (nx = 0; nx < x; nx++)
        {
            printf("\n");
            for (ny = 0; ny < y; ny++)
            {
                printf("%d  ", monTableau[nx][ny]);
            }
        }
        printf("\n");}
    }
    return 0;
}